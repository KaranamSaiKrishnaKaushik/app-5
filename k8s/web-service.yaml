#apiVersion: v1
#kind: Service
#metadata:
#  name: web-service
#  namespace: app-5-dev
#spec:
#  type: NodePort
#  selector:
#    app: angular-app-5
#  ports:
#    - protocol: TCP
#      port: 80
#      targetPort: 80
#      #nodePort: 30084
#  type: NodePort
#  externalTrafficPolicy: Cluster
#  sessionAffinity: None

apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: app-5-dev
spec:
  type: NodePort
  selector:
    app: angular-app-5
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30081