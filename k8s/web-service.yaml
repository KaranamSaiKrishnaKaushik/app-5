apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: app-5-dev
spec:
  selector:
    app: angular-app-5
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
